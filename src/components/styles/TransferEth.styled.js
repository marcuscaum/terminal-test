import styled from 'styled-components';
import { Drawer } from '@material-ui/core';

export default styled(Drawer)`
  > div {
    padding: 15px;
    overflow: hidden;
  }

  hr {
    margin-bottom: 15px;
  }
`;
